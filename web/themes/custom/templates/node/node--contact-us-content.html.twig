<section id="" class="contact-us common_bg">
    <div class="container reveal active">
        <div class="row">
            <div class="row contact-block">
                <div class="col-md-6 contact-left pe-lg-5">
                    <h3 class="mb-sm-4 mb-3">Contact Info</h3>
                    <p class="cont-para mb-sm-5 mb-4">
                        {{ content.field_message|raw }}
                    </p>

                    <div class="cont-details">
                        <div class="contact-item"><i class="fas fa-map-marker-alt"></i>
                        <span class="field">{{ content.field_address|without('label')|raw }}</span></div>
                        <br>
                        <div class="contact-item"><i class="fas fa-phone-alt"></i>
                        <span class="field">{{ content.field_telephone|without('label')|raw }}</span></div>
                        <br>
                        <div class="contact-item"><i class="fas fa-envelope-open-text"></i>
                        <span class="field"><a href="mailto:{{ content.field_email.0.value }}" class="mail">{{ content.field_email|raw }}</a></span></div>
                    </div>
                </div>

                <div class="col-md-6 contact-right mt-md-0 mt-5 ps-lg-0 ContactPageForm">
                    
                    {# ✅ Success/Error Messages Display #}
                    {% if messages %}
                        <div class="messages">
                            {{ messages }}
                        </div>
                    {% endif %}

                    <div class="custom-webform">
                        {{ { '#type': 'webform', '#webform': 'contact_form' } }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

{# ✅ Add CSS for Styling Messages #}
<style>
    .messages {
        padding: 15px;
        margin-bottom: 20px;
        border-radius: 5px;
    }
    .alert-success {
        background-color: #d4edda;
        color: #155724;
        border: 1px solid #c3e6cb;
        padding: 10px;
    }
    .alert-error {
        background-color: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
        padding: 10px;
    }
</style>
