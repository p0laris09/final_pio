{% if items %}
  <ul>
    {% for item in items %}
      <li{{ item.attributes }}>
        {{ item.title }}
        {% if item.below %}
          {{ item.below }}
        {% endif %}
      </li>
    {% endfor %}
  </ul>
{% endif %}

